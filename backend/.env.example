# Production Settings
ENV=production
DATABASE_URL=sqlite:///./filmBox.db
LOG_LEVEL=INFO

# Feature Flags
ENABLE_LLM=false

# External APIs (Required only if ENABLE_LLM=true)
GEMINI_API_KEY=your_gemini_api_key_here
